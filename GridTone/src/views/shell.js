/** Single composition workspace. The lower editor is a panel, never a second page. */
(function(G){G.views||={};
 G.views.renderShell=function(C){
  const {project,S,B,engine,history,future,button,ib,esc,icon,renderEditor,renderArrange,renderMix,scrollKey}=C;
  const editor=()=>S.editorOpen?`<div class="dock-resize" role="separator" tabindex="0" aria-label="调整音符编辑器高度" aria-orientation="horizontal" aria-valuemin="240" aria-valuemax="900" aria-valuenow="${S.editorHeight}"></div><div class="creation-workspace" id="creation-workspace"><section class="studio-editor" style="height:${S.editorHeight}px" aria-label="音符编辑器">${renderEditor()}</section></div>`:'';
  const panel=['creation','composer'].includes(S.rightPanel)?C.creationMarkup:C.inspectorMarkup;
  const content=S.view==='mix'?`<section class="mix-workspace"><header class="section-heading"><h2>混音</h2>${button('creation','建议混音','spark','soft-btn','data-mode="mix"')}</header>${renderMix()}</section>`:`<section class="studio-arrangement" id="arrangement-pane"><header class="section-heading"><div><h2>编排</h2><small>${project.tracks.length} 条音轨 · ${project.bars} 小节</small></div><div class="button-row">${button('add-track','音轨','plus','quiet')}${button('new-blank-clip','空白音乐块','plus','quiet')}${button('structure-tools','编排工具','arrange','quiet')}${button('time-tools',project.bars+'小节 · 长度','range','quiet')}${ib('add-bars','增加4小节','plus')}${!S.editorOpen?button('edit-open','展开编辑器','pencil','soft-btn','title="显示已选或此前打开的音乐块"'):''}</div></header>${renderArrange()}</section>${editor()}`;
  return `<main class="app-shell studio-shell legou-shell single-workspace ${S.editorExpanded&&S.editorOpen&&S.view!=='mix'?'editor-expanded':''}" style="--studio-dock-height:${S.editorHeight}px"><header class="topbar"><div class="brand-area"><a class="brand" data-action="home" href="#" aria-label="乐构 · 编排"><span class="brand-sign" aria-hidden="true"><i></i><i></i><i></i></span><b>乐构</b></a><button class="project-name" data-action="rename-song" title="重命名作品">${esc(project.title)} ${icon('pencil',12)}</button>${ib('project-menu','我的作品','folder')}</div><div class="top-transport" aria-label="播放控制">${button('play','',engine.playing?'pause':'play','play-btn',`aria-label="${engine.playing?'暂停':'播放'}"`)}${ib('stop','停止 · 再按回曲首','stop')}<label class="tempo"><input id="bpm" type="number" min="40" max="240" data-field="bpm" value="${project.bpm}" aria-label="速度 BPM"><span>BPM</span></label></div><nav class="workspace-tabs" role="group" aria-label="工作视图">${button('view','编排','arrange',S.view==='arrange'?'active':'quiet','data-view="arrange"')}${button('view','混音','mix',S.view==='mix'?'active':'quiet','data-view="mix"')}</nav><div class="top-actions">${ib('undo','撤销 · '+G.keymap.label('undo')+'','undo',history.length?'':'disabled')}${ib('redo','重做 · '+G.keymap.label('redo')+'','redo',future.length?'':'disabled')}${button('library-open','音乐模板','grid','quiet','data-kind="music"')}${button('export','导出','upload','quiet')}${ib('workspace-menu','作品与设置','dots')}</div></header>
  <div id="editbar-host">${G.views.renderEditBar(C)}</div>${G.missingResources(project).length?`<div class="missing-banner" role="status">部分音源待补齐 ${button('import-catalog','导入素材包','upload','quiet')}${button('open-sound','替代音色','wave','quiet')}</div>`:''}
  <div class="workspace-body"><div class="view-content" data-scrollkey="${esc(scrollKey)}" data-workspace="${S.view}">${content}</div><aside id="creation-dock" class="creation-dock" aria-label="当前工具面板" ${panel?'':'hidden'}>${panel||''}</aside></div>
  ${C.shelfMarkup||''}<footer class="playback-footer"><div class="footer-loop">${button('loop','循环','loop',B.loop?'on':'quiet',`aria-pressed="${B.loop}"`)}<div class="transport-loop"><input type="number" data-field="loop-from" aria-label="循环起始小节" min="1" max="${project.bars}" value="${(B.range?.[0]||0)/G.BAR+1}"><span>—</span><input type="number" data-field="loop-to" aria-label="循环结束小节" min="1" max="${project.bars}" value="${(B.range?.[1]||project.bars*G.BAR)/G.BAR}">${ib('clear-range','清除循环范围','close')}</div></div><div class="clock"><strong id="position">001 : 01 : 01</strong><small id="playback-label">${esc(G.playbackLabel(project,S,B))}</small></div>${button('clear-solo','听全曲','headphones','quiet')}<span id="storage-status" role="status" aria-live="polite"></span><span id="save-status-actions" class="save-status-actions" hidden>${button('retry-save','重试','save','quiet small-btn')}${button('export-project','备份','download','quiet small-btn','aria-label="下载作品文件备份"')}</span><span id="runtime-status"></span>${ib('appearance','外观','spark')}${ib('help','操作帮助','help')}</footer></main>`;
 };
})(globalThis.GridTone||={});
