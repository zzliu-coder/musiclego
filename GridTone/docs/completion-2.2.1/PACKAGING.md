# 精简交付策略

本次提供小型应用ZIP和可构建源码ZIP。运行代码、完整音乐目录、授权和构建/测试输入全部保留。源码包只排除.git工作目录、缓存，以及大批测试产生的WAV。当前音频检查实际完整执行102项，保留JSON测量、哈希、26份组合工程和生成脚本；正常脚本可再次生成WAV。浏览器实际下载的MIDI与工程保留。

历史文本与测试输入保留；历史证据代表其原版本。本轮中间失败日志保留用于追溯：一次跨VM数组比较的测试断言归一化问题，两次fixture遗漏pipeline.rate/无操作撤销的错误假设已修正；最终实际生产音乐断言没有降低。原来批量立即应用的旧页面断言改为“固定计划→确认”，作为显式交互规则变化记录。

新的Source manifest逐文件列出SHA-256。Verification在包外记录最终ZIP摘要。完整源码从实际ZIP独立解压后重新执行npm test与build；应用HTML与源码产物逐字对照。补丁和本地Git保存点单独验证，不推送远端。
