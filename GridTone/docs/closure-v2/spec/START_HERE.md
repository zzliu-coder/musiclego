# 实施交接入口

先读 IMPLEMENTATION_MASTER_PLAN.md，再读 ACCEPTANCE_LEDGER.json。

本次任务是C0—C11完整范围，四批旧交互建议已合入。原87项与82项新验收都要在最终版关闭。不要增加教学模式。已有有效能力保留并复验，勿为目录拆分更换框架或重写引擎。

## 每批操作

核对固定HEAD与未提交工作；阅读本批前置；先建立/还原相关成功路径；按职责实现；执行本批与受影响旧回归；记录证据；通过后建立close-cN保存点。模板/音源需要真实试听，设备范围按C10处理，任何环境阻断如实保留。不得把未做功能移到限制文件后声称完工。

## 结束条件

原87项和新增82项全部required PASS且有最终构建证据；29条需求映射关闭；已知范围内未关闭问题0；源码包独立重建与交付HTML一致。到C11结束，不再留下未列出的下一批。

## 计划工具

python verify_plan.py --coverage

用于检查计划结构，不运行应用。实施后填写ACCEPTANCE_LEDGER.json及相对证据文件，再运行：

python verify_plan.py --release

原始计划未施工时，此命令会非零退出，属于预期。真正应用的测试入口由C0/C11统一，不凭这个检查器证明应用正确。
