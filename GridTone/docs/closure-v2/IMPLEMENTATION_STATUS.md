# 全范围收尾 v2 · 实施状态

当前为施工与验证候选，尚未宣称完整发行验收通过。

## 用户确认的范围

- macOS 桌面。用户明确排除 Android 真机和 Windows 浏览器。
- 小屏布局以后专题，本轮不实现、不作为阻塞项。
- 听感验收仍按实际结果记录，自动渲染不能替代人工审听。

## 已接入的主要实现

- 三个工作区、常驻素材架、非模态创作面板；片段与编排共用状态。
- 统一候选状态、固定目标、局部冲突检查补丁、绝对预听范围。
- 跨界持续音与实际演奏层依赖、固定声部计算顺序。
- 贝斯实际音域、鼓风格与力度、伴奏弹奏方式；演奏起音补偿和烘焙。
- 可复用节奏骨架、动机连接、回答句、收束、邻音解释。
- 素材拖放／点击／键盘入口、边界长音确认、独立共享实例、落点滚动。
- 麦克风授权迟到取消、拒绝恢复、启动失败释放；结构化提交错误。

## 验证范围

旧版本证据保留在 docs/implementation；新证据集中在 evidence/current。
基线六项问题的复现针对固定 98d4610，不作为新版本通过证据。
候选构建以 dist/manifest.json 身份为准；浏览器、真实音源、Safari、长期负载分别记录。
严格门禁 scripts/verify-closure-gate.mjs 会拒绝缺项、失败、旧哈希、无证据及未授权豁免。

## 收尾仍需落实

- 最终固定构建的30分钟长期负载正在运行；完整自动回归、Chrome / Firefox 与原生 Safari 已通过。
- 全量音色／配方／代表生成的听评，保留真实结论。
- 逐条审计 169 项总账及 29 项问题映射，不能以总测试数代替逐项结果。
- 样式迁移的12种桌面状态计算样式对比已通过；独立归档重建、打包与 GitHub 推送继续进行。
