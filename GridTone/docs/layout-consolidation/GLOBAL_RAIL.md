# 全局编辑栏与编排结构归位

2026-09-20。范围：当前编排工作台及内嵌编辑器。沿用暖白轻拟物、既有选区/命令模型，不扩展其他页面设计，不修改音乐文档格式或声音。工作分支 codex/legou-tactile-2-2-1；保留原有未提交改动；本轮未提交或推送。

## 实现

- 顶部 DOM/键盘顺序：作品入口、播放、当前对象＋基础编辑＋生成、撤销/重做/模板、编排/混音切换。
- 添加音轨、空白音乐块、编排工具和长度回到编排标题栏；全页仍只有一个编辑命令宿主，不复制按钮或事件控制器。
- 全局栏使用完整外面板，内部通过间距、细分隔线组织；不再使用参差的独立灰底托盘。画板原有工具分组保留。
- 当前对象使用可展开原生 details；显示简短范围，展开可查看完整名称及粘贴落点。未选音符明确写出“音符 · 未选中”。对象详情保留单音乐块的“编辑”入口；生成仍直接可见。
- 粘贴按钮 title 同时保留快捷键、禁用原因与落点。详情支持点击外部关闭、Escape 关闭后返回摘要焦点。
- 761—1390px 收起基础编辑按钮的重复图标，保留完整文字和可访问名称；其他工具不隐藏。窄屏按 DOM 顺序换行，未减小文字字号。composer 的保护边界覆盖移入顶部的编辑模块及编排区域。
- 生产组件页继续使用实际 renderEditBar 的五种对象样例；覆盖表和 CW01—CW10 测试同步更新。

## 当前验证

PASS：452 项核心测试；构建；源样式审计；git diff --check；布局检测无报告项。

实际 Codex 内置浏览器，127.0.0.1:8001，最终截图位于 evidence/global-rail/：

| 视口宽度 | 顶栏高度 | 编排标题栏高度 | 顶部与结构按钮横向越界 |
|---|---:|---:|---|
| 1440 | 60 | 44 | 无 |
| 1280 | 60 | 44 | 无 |
| 1100 | 120 | 44 | 无 |
| 760 | 180 | 44 | 无 |
| 390 | 273 | 91 | 无 |

1440、1280 单排；1100 及以下按功能组换行。极窄窗口仍以可达为目标，需要纵向滚动，不宣称手机具有桌面同等密度。

实际交互：

- 音乐块复制后粘贴按钮显示目标小节；复制一份使 8 小节增长为 16 小节，点击撤销后项目 JSON 与操作前逐字一致。
- 聚焦画布并使用全选快捷键，顶部对象与复制按钮名称更新为 48 个音符。
- 对象详情展开、Escape 关闭、焦点回到 summary，实际聚焦外圈 2px。
- 播放引擎 playing=true，停止后 false。
- 1280 下音色侧栏打开后顶栏仍单排；音轨目标的较长“音轨属性”标签仍不溢出。
- 混音/编排来回切换、模板库打开/返回正常；混音中没有音乐编辑命令栏。
- 页面控制台 error 为空。测试后恢复正常视口，预览保留并显示已保存在本机。

构建：2937991 bytes；SHA-256 fdae709e9c42d5aae126baddbfdd638708e462cc29f90e1ea1846c8ec6f31b54。

## 边界

截图和操作由主代理完成，本轮无新增子代理验收。没有执行完整 verify:engineering 的 600 秒持续检查、verify:release、触屏实机或主观听评。图片说明视觉状态，不代替这些验收。旧 COMPACT_WORKBENCH.md 中的托盘顺序及先前构建摘要仅为历史迭代证据。
