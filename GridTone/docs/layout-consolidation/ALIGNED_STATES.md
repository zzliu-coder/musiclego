# 双编辑栏对齐与选中状态｜2026-09-20

范围：当前 compact-workbench。顶部全局布局保留；其他页面尚未推广。没有修改音乐数据结构、音频引擎或命令实现，没有提交或推送 GitHub。

## 实现

- 编排结构栏与画板栏共用 `--workbench-identity: var(--track-head)`，216px 标题列。标题左缘、主要工具起点一致；音高尺仍保持原有窄宽度。
- 两栏标题均按名称／辅助说明两层排版。宽容器工具栏最低52px；不足宽度时按完整功能组换行，DOM和键盘顺序保持。
- 容器1400px起采用单排结构；分页和触屏等较宽组合可以在辅助组内部换行。720px以下标题和工具改为共同左缘的单列结构。
- 网格／时间／音域移除重复灰底，以间距和细分隔线分组。三连音选择框112px，分页选择框104px，避免截断选项名称。
- 模式与开启的按钮在本工作台内共用浅珊瑚底、深珊瑚文字、透明边界和浅内阴影。悬停维持开启语义；禁用不套用开启阴影。播放／停止的实体感保持。
- 音乐块选择与键盘焦点仍各自使用现有语义和外框；音乐块原有轨道颜色与材质未更改。
- 生产组件样例、设计系统、覆盖表和 CW11/CW12 测试同步更新。

## 实际浏览器验收

通过 Codex 内置浏览器访问真实 `http://127.0.0.1:8001/` 源码入口，重新加载后检查。

| 检查 | 结果 |
|---|---|
| 1440/1600桌面 | 两栏标题 x=24，工具 x=248，误差0px；默认和三连音时画板栏52px |
| 容器1400/1399边界 | 分别52px单排／93px两排；控件无水平越界 |
| 1280/1100窗口 | 216px标题列、工具起点保持；整组换行，控件无水平越界 |
| 760/600窗口 | 窄宽重排可达，600时两栏标题和工具共同左缘x=22；无控件水平越界 |
| 三连音＋分页 | 控件无水平越界；发现分页名称宽度不足，修为104px并实际复核 |
| 粗指针模拟 | 画板图标按钮44×44px；辅助组可换行，无水平越界。仅浏览器模拟，不是真实触屏验收 |
| 选中样式 | 编排、画笔、循环、M、S 计算样式同为 `rgba(124,36,64,.14) 0px 1px 2px inset` |
| 工具／循环 | 选音符切换正确；循环关闭恢复实体按钮阴影，再开启恢复内阴影 |
| 键盘 | 从选音符按Tab，时间选区获得独立2px蓝色外圈 |
| 音乐对象 | 所选音乐块保留蓝色外框、杏色轨道面与磁贴厚度 |
| 编辑／播放回归 | 复制可粘贴；复制一份8→16小节，撤销回原稿；播放显示暂停，停止恢复播放按钮 |
| 原稿保持 | 上述测试恢复后，`JSON.stringify(GridToneApp.getProject())` 与测试前完全一致 |
| 页面异常 | 本轮浏览器控制台error为空 |

截图在 `evidence/aligned-states/`：desktop-1440、keyboard-focus、triplet-1440/1280/760/600、paged-triplet-coarse。分页控件修为104px后已重新加载并更新该组合截图；验收结束恢复连续显示、1/16网格和默认窗口尺寸。

## 独立检查与边界

- 子代理 `aligned_states_acceptance` 只读检查CSS优先级、焦点、触屏尺寸、布局顺序与作用域，结论PASS；定向测试29/29。它未操作浏览器，图像验收由主代理完成。
- 首轮核心测试发现新增测试正则漏写样式空格，修正断言；未降低行为要求。随后454项通过。
- 源码设计检查PASS，Impeccable布局扫描无发现。
- 不把上述局部验收算作完整600秒工程套件、全平台发行验收或所有页面重设计完成。
